/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.postimg.cc', 'img.icons8.com', 'cdn-icons-png.flaticon.com',
        'www.flaticon.es'], 
    },
    // ... otras configuraciones de Next.js, si las tienes
  };
  
  module.exports = nextConfig;