/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.postimg.cc'], // Agrega el dominio de tus imágenes aquí
    },
    // ... otras configuraciones de Next.js, si las tienes
  };
  
  module.exports = nextConfig;